#include "main.h"

/**
 * _abs - computes  the absolute value of an integerr
 * @n: thhe int to check;
 * Return: the absolutee valuee of intt
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
