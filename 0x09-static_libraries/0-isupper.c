#include "main.h"

/**
 * _isupper - checksss for uppercassse character
 * @c: the character to be checkeeed
 * Return: 1 if c is uppercase, 0 otherwissse
 */
int _isupper(int c)
{
	return (c >= 'A' && c <= 'Z');
}
