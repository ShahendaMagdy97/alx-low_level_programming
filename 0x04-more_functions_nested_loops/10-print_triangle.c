#include "main.h"

/**
 * main - check the code
 *
 * Return: Always 0 (Success)
*/

void print_triangle(int size);
{
	print_trianglr(2);
	print_trianglr(10);
	print_trianglr(1);
	print_trianglr(0);
	return (0);
}
